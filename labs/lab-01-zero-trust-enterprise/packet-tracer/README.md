# Packet Tracer Artifact

Place the final `.pkt` file in this directory when it is ready for publication.

Recommended filename:

```text
lab-01-zero-trust-enterprise.pkt
```

Before publishing it:

- correct the IT host default gateway to `10.10.40.1`;
- confirm endpoint labels and physical access-port assignments;
- save all device configurations;
- repeat the validation matrix;
- ensure that all credentials remain lab-only.

